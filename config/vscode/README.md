# Editor configuration

`setup.sh` links this directory into the VS Code user-config directory. Cursor
has a separate canonical configuration under `config/cursor` so each editor can
retain its own theme and application-specific commands.
